# Unternehmensprogramm
Enterprise Resource Management der Werft.
install using pip to virtual enviroment . 

install those dependency:
Django==2.0.13
django-ckeditor==6.0.0
Pillow==7.2.0
xlrd==1.2.0

Manually Add These Things:
1. static
2. db.sqlite3
3. venv

Then Check Host IP Address if ip address OK then run server with 0.0.0.0:port number.

N.B: Follow .gitignore file
